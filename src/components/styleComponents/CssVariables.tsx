import { createGlobalStyle } from 'styled-components';

const CssVariables = createGlobalStyle`
     :root {
          --primary-color: #209cf4;
          --white-color: #ffffff;
     }
`;

export default CssVariables;
